#set page(paper: "a4", margin: 2cm)
#set text(font: "Liberation Sans", size: 12pt)

= Ahoy, Mateys!

This be a test document, compiled by the embedded Typst compiler.

== The Crew
- *Captain*: Context Pilot
- *First Mate*: Callback Engine
- *Navigator*: Package Resolver

== The Treasure
This paragraph was added by an edit — the callback should recompile automatically!

== The Mission
Verify that `.typ` files auto-compile to `.pdf` in the same directory,
with no config.json, no output folder, and no unnecessary complexity.

#align(center)[
  #text(size: 18pt, weight: "bold")[⚓ Fair winds and following tests! ⚓]
]
